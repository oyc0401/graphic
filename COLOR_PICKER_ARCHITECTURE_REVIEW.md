# 색 선택 도구 개발 회고

이 문서는 색 선택 도구를 추가하면서 확인한 코드 아키텍처의 좋았던 점과 아쉬웠던 점을 정리한다. 기능 자체는 작지만, 입력 이벤트, 도구 상태, WebGL 픽셀 접근, UI 표시, 선택 영역 적용 흐름을 모두 지나가기 때문에 현재 구조의 장단점이 꽤 잘 드러났다.

## 좋았던 점

### 도구 레이어가 비교적 명확하게 분리되어 있음

앱 레벨의 도구는 `src/app/tools` 아래에 모여 있고, `toolRegistry`가 현재 `paintState.activeToolId`에 맞는 도구 객체로 포인터 이벤트를 분배한다. 덕분에 색 선택 도구도 `ColorPickerTool` 클래스로 독립시켜 `down`, `move`, `up`, `cancel` 생명주기를 기존 브러시/선택 도구와 같은 방식으로 구현할 수 있었다.

이 구조의 장점은 새 도구를 추가할 때 전체 포인터 이벤트 흐름을 크게 건드리지 않아도 된다는 점이다. 색 선택 도구처럼 실제 그리기 작업을 만들지 않는 도구도 같은 인터페이스에 맞춰 들어갈 수 있었다.

### UI 상태와 코어 렌더링 상태가 분리되어 있음

`paintState.selectedToolId`와 `paintState.coreTool`이 분리되어 있어서 색 선택 도구를 "UI 전용 도구"로 넣을 수 있었다. 색 선택은 코어 WebGL 도구가 아니라 현재 레이어에서 픽셀을 읽고 앱의 색 상태를 바꾸는 기능이다. 그래서 내부 코어 도구는 `brush`로 정리하면서, UI 선택 상태만 `colorPicker`로 유지하는 구현이 가능했다.

이 분리는 선택 도구, 줌 도구, 색 선택 도구처럼 "렌더러의 브러시 도구와 1:1로 대응하지 않는 입력 모드"를 표현하는 데 유리하다.

### 색 상태가 중앙화되어 있음

색 변경은 `colorState.setColorFromRGB()` 하나로 들어가면 된다. 색 선택 도구가 HSV 변환, hex 입력 표시, 색상 인디케이터 갱신을 직접 알 필요가 없었다. `colorState`가 RGB 입력을 받아 내부 HSV와 입력 텍스트까지 갱신하기 때문에, 도구 구현은 "픽셀에서 RGB를 얻는다"에 집중할 수 있었다.

### WebGL 접근 지점이 한 군데로 모일 수 있음

색 샘플링은 `RendererInterface`와 `WebGL2Controller`, `PaintService`를 통해 코어 쪽 API로 추가했다. 앱 레벨 도구가 WebGL 컨텍스트나 FBO를 직접 만지지 않고 `getLayerWorker().sampleColor(x, y)`만 호출하는 형태가 되었다.

이 방향은 좋다. 이후 Canvas2D/WebGPU 렌더러가 생겨도 앱 도구는 같은 API를 호출하고, 렌더러별 구현만 바꾸면 된다.

## 아쉬웠던 점

### 도구 변경 흐름이 여러 책임을 동시에 가진다

`toolManager`의 도구 변경 함수는 단순히 선택 도구 id를 바꾸는 것 이상을 한다. 선택 영역 적용, 픽셀유동화 세션 적용 여부 확인, 코어 도구 변경, 히스토리 상태 동기화까지 같이 처리한다.

색 선택 도구를 처음 넣을 때 이 흐름을 우회하자 선택 영역이 남는 문제가 생겼다. 반대로 `setCoreTool()`을 직접 호출하면 기존 도구 변경 규칙 일부를 건너뛰게 된다. 결과적으로 "도구 변경"과 "현재 작업 정리"가 강하게 결합되어 있다는 점이 드러났다.

개선 방향은 도구 전환 전처리를 별도 함수로 분리하는 것이다. 예를 들어 `prepareTransientTool()` 또는 `exitCurrentEditableState()` 같은 함수가 선택 영역 적용과 세션 정리를 담당하고, 각 도구 진입 함수는 그 결과를 이용해 UI 상태만 바꾸는 식이 더 명확하다.

### UI 전용 도구와 코어 도구의 관계가 타입으로 드러나지 않음

현재 `ToolId`에는 `brush`, `select`, `selection`, `zoom`, `colorPicker`가 같이 들어 있고, `CoreTool`에는 `brush`, `eraser`, `liquify`, `select`, `selection`이 들어 있다. 두 타입의 관계는 `toolIdForCoreTool()`과 `paintState.brushId` 같은 분기 로직에 흩어져 있다.

색 선택 도구는 `ToolId`에는 있지만 `CoreTool`에는 없는 도구다. 이런 종류가 더 늘어나면 `paintState.coreTool`, `paintState.toolId`, `paintState.activeToolId`, `inputMode`의 조합을 머릿속으로 추적해야 한다.

개선 방향은 도구 메타데이터를 레지스트리에 모으는 것이다. 예를 들어 각 도구가 `kind: "core" | "transient" | "viewport"` 또는 `coreTool?: CoreTool` 같은 정보를 갖게 하면, 도구 전환과 UI 선택 상태 처리가 더 선언적으로 바뀐다.

### 포인터 이벤트의 우선순위가 도구별로 명확하지 않음

`pointerEvents.dispatch()`는 PAN, resizeTool, active tool 순서로 처리한다. 색 선택 도구를 추가하자 리사이즈 핸들이 색 선택보다 먼저 실행될 수 있었다. 결국 `paintState.toolId !== "colorPicker"` 조건을 리사이즈 처리와 리사이즈 표시 조건에 각각 추가해야 했다.

이 방식은 기능이 늘수록 예외 조건이 퍼질 수 있다. 특정 도구가 리사이즈 핸들을 허용하는지, 캔버스 밖 입력을 허용하는지, PAN보다 우선하는지 같은 정책이 도구 정의에 가까이 있어야 한다.

개선 방향은 도구별 capability를 두는 것이다. 예를 들어 `allowsCanvasResizeHandle`, `usesBrushCursor`, `samplesDuringDrag` 같은 속성을 도구 레지스트리에 두면 이벤트 분배와 UI 표시가 같은 정책을 공유할 수 있다.

### 포인터 생명주기와 도구 전환 타이밍이 민감함

색 선택 도구는 클릭 후 자동으로 브러시로 돌아가야 한다. 처음에는 `down`에서 바로 브러시로 바꿨지만, 그러면 같은 포인터 시퀀스의 `up`이 브러시 도구로 들어갈 수 있다. 이후 `up`에서 바꾸도록 수정했지만, `toolManager.setBrushTool()`은 `paintState.pointerdown` 가드가 있어 synthetic up이나 핀치 전환 같은 경우에 타이밍 문제가 생길 수 있었다.

최종적으로는 `up`에서 다음 이벤트 턴으로 복귀를 미루는 방식으로 해결했다. 동작은 맞지만, 포인터 상태 변경과 도구 상태 변경 순서가 암묵적이라 취약하다.

개선 방향은 "일회성 도구" 개념을 명시하는 것이다. 예를 들어 도구가 `returnToolAfterPointerUp: "brush"`를 선언하면, 포인터 시스템이 모든 포인터 종료 처리를 마친 뒤 안전한 시점에 복귀시키는 구조가 더 안정적이다.

### 색 샘플링의 색공간/합성 규칙이 API 이름에 드러나지 않음

`sampleColor()`는 현재 레이어에서 1픽셀을 읽고, 흰색 배경 합성 이후의 RGB를 반환한다. 이 기능 요구에는 맞지만 API 이름만 보면 원본 픽셀 RGB인지, 알파 포함인지, 흰 배경 합성값인지 알기 어렵다.

개선 방향은 반환 타입이나 함수명을 더 명확히 하는 것이다. 예를 들어 `sampleColorOnWhite()` 또는 `{ r, g, b, a, compositedOn: "white" }`처럼 규칙을 드러내면 이후 투명 픽셀 정책을 바꿀 때 혼동이 줄어든다.

## 정리

이번 작업에서 가장 좋았던 점은 새 도구를 독립 클래스로 추가하고, 색 상태와 렌더러 API를 기존 구조에 자연스럽게 연결할 수 있었다는 점이다. 반면 가장 아쉬웠던 점은 도구 전환 전처리, 포인터 우선순위, UI 전용 도구의 정책이 여러 파일에 분산되어 있어 작은 기능에도 예외 조건이 늘어났다는 점이다.

다음에 도구가 하나 더 추가된다면, 먼저 도구 레지스트리에 메타데이터를 추가하고 이벤트/커서/리사이즈/자동 복귀 정책을 그 메타데이터에서 읽도록 정리하는 것이 좋다.
