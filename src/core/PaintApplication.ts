export function startApplication(
  main_canvas: OffscreenCanvas,
  screenWidth: number,
  screenHeight: number,
  dpr: number,
  width: number,
  height: number,
  px: number,
  py: number,
  scale: number
) {}

/// 기존 paintOption은 일정하게 저장하고 webgl과 같이 특수한 좌표계에서는 plugin을 개발해서 사용.
