brush, eraser (브러시)
형광펜 (브러시)
물감 (브러시, 이건 중복된 부분 칠하면 덮어씌워짐.)
도트연필 (브러시, 브러시크기영향x, 픽셀단위 선)

select, selection (선택창, 사실 도구라고 하기 뭐함.)
자유형 선택 (선택창, 브러시로 일부 그리면 그 내부 영역 선택)

liquify-push (픽셀유동화 세션, 브러시)
liquify-spin (픽셀유동화 세션, 스프레이형식)
liquify-reverseSpin (픽셀유동화 세션, 스프레이형식)
liquify-restore (픽셀유동화 세션, 브러시, 브러시)

모자이크 (모자이크 세션, 브러시)
범위 모자이크 (모자이크 세션, start-end정하면 그사이 모자이크)
자유형 모자이크 (모자이크 세션, 브러시로 일부 그리면 그 내부 영역 모자이크)

zoom (캔버스 영향X, 임시도구 가능)
colorPicker (캔버스 영향X, 임시도구 가능)
floodFill (down만 있는 도구)


startShape(shape);
  createRectShape(px: number, py: number, width: number, height: number): void;
  createEllipseShape(px: number, py: number, width: number, height: number): void;

startLine();
  createLineShape(p1: Pointer, p2: Pointer): void;
  createCurveShape(p1: Pointer, p2: Pointer, c1 :Pointer): void;

  applyShape(): void;
  discardShape(): void;
